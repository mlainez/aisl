(module test_simple_fail
  (fn returns_five -> int
    (ret 5))
  
  (test-spec returns_five
    (case "expects 10 but gets 5"
      (input)
      (expect 10)))
  
  (meta-note "Simple test that should fail and show 5"))
