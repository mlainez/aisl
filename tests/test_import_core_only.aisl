(module test_import_core_only
  (import math)
  
  (fn main -> int
    (set x int 10)
    (set y int 5)
    (set result int (call add x y))
    (call print result)
    (ret 0))
  
  (meta-note "Tests explicit import but using only core functions"))
