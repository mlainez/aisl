(module test_validation_simple
  (import validation)
  
  (fn main -> int
    (call print "Testing validation")
    (set result bool (call is_positive 42))
    (if result
      (call print "PASS: is_positive works")
      (call print "FAIL: is_positive broken"))
    (ret 0)))
