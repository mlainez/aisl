(module test_http_and_json
  (import http)
  (import json_utils)
  
  (fn main -> int
    (call print "Testing HTTP:")
    (set status_text string (call get_status_text 200))
    (call print status_text)
    
    (call print "Testing JSON:")
    (set obj json (call build_object_one "key" "value"))
    (set str string (call to_string obj))
    (call print str)
    
    (ret 0)))
