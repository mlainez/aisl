(module echo_terminal
  (fn main -> int
    (call print "Echo terminal - type something and press Enter (Ctrl+C to quit)")
    (loop
      (set line string (stdin_read))
      (call print line))
    (ret 0)))
