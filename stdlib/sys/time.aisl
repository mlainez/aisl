(module time
  (fn now -> int
    (ret (time_now)))
  
  (fn format timestamp int fmt string -> string
    (ret (time_format timestamp fmt)))
  
  (fn parse time_str string fmt string -> int
    (ret (time_parse time_str fmt))))

